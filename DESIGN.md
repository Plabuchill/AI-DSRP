# Design System — AI Disease Surveillance & Response Platform

โทนหลัก: **Earth Tone + Minimalist + Muji-inspired** — เรียบ สงบ เป็นธรรมชาติ ไม่ตกแต่งเกินความจำเป็น (form follows function) แต่ยังต้องอ่านสถานะวิกฤต/แจ้งเตือนได้ชัดเจนในระบบที่เกี่ยวกับสุขภาพ

> ไฟล์นี้แทนที่ดราฟต์ก่อนหน้า (โทน Clinical/Trustworthy สีฟ้า-เขียว) — Prototype `v1` ที่สร้างไปแล้วยังอ้างอิงพาเลตต์เก่าอยู่ ถ้าต้องการให้ตรงกับ DESIGN.md ฉบับนี้ ต้องเรียกให้อัปเดต prototype อีกครั้ง (ระบบจะถามว่าจะสร้าง `v2` ใหม่ หรือแก้ `v1` เดิม)

---

## 1. Brand Identity & CI

- **ชื่อโปรเจกต์/ผลิตภัณฑ์**: AI Disease Surveillance & Response Platform
- **Wordmark**: ข้อความล้วน "AI-DSRP" ตัวพิมพ์เล็กทั้งหมด (`ai-dsrp`) น้ำหนัก medium ไม่มีไอคอน/สัญลักษณ์ประกอบ — สะท้อนความเรียบง่ายแบบ Muji ที่ไม่ต้องมีโลโก้ตราสัญลักษณ์หวือหวา (ถ้ามีโลโก้จริงในอนาคต ให้แทนที่ตรงนี้และคง wordmark เป็นตัวสำรอง)
- **บุคลิกแบรนด์ (Brand Personality)**: สงบ, ตรงไปตรงมา, น่าไว้วางใจแบบเงียบๆ (quiet confidence) — ไม่ใช้ภาพลักษณ์ที่ดูตื่นตระหนกหรือ "ขายของ" แม้จะเป็นระบบเฝ้าระวังโรคที่ข้อมูลอาจอ่อนไหว เป้าหมายคือให้ผู้ใช้รู้สึกว่าเครื่องมือนี้ "ควบคุมได้ ไม่ใช่แหล่งความวิตกกังวล"
- **น้ำเสียง/ภาษา (Voice & Tone)**: ข้อความสั้น ตรงประเด็น ไม่ใช้ศัพท์เทคนิคเกินจำเป็น ไม่ใช้เครื่องหมายตกใจ/คำที่สร้างความตื่นตระหนกเกินจริง แม้ในข้อความแจ้งเตือนระดับวิกฤต ให้ใช้คำสุภาพหนักแน่น (เช่น "ต้องตรวจสอบด่วน" ไม่ใช่ "อันตราย!!! ด่วนที่สุด!!!")
- **แรงบันดาลใจ**: MUJI — วัสดุ/สีจากธรรมชาติ, ไม่มีลวดลายเกินจำเป็น, ให้ "ช่องว่าง" (whitespace) ทำหน้าที่จัดระเบียบแทนเส้นขอบ/สีสันจัดจ้าน

---

## 2. Design Tokens

### 2.1 Colors

พาเลตต์หลักเป็นโทนดิน (earth tone) อิ่มตัวต่ำ ยกเว้นสีสถานะ (semantic) ที่จงใจให้อิ่มตัวสูงกว่าพื้นหลังพอสมควร เพื่อให้แยกแยะสถานะเฝ้าระวัง/วิกฤตได้ชัดแม้จะอยู่ในภาพรวมที่เรียบสงบ

| Token | Hex | ใช้ตรงไหน |
|---|---|---|
| primary | #7A6A53 | ปุ่มหลัก, ข้อความ/ไอคอนที่ต้องการเน้น, active state |
| secondary | #8A9A5B | องค์ประกอบรอง, ไอคอนสถานะทั่วไป (sage green) |
| accent | #C9A66B | จุดเน้นเบาๆ, highlight บนกราฟ/แผนภูมิ (ochre/มัสตาร์ด) |
| success | #6B8E5A | สถานะปกติ/ควบคุมได้ (moss green, อิ่มตัวสูงกว่าโทนหลักเล็กน้อยเพื่อความชัดเจน) |
| warning | #C08A3E | สถานะเฝ้าระวัง (amber ดิน) |
| danger | #B5453A | สถานะวิกฤต/error/เกิน threshold (terracotta-red, ใช้เฉพาะสถานะจริงเท่านั้น) |
| background | #F5F1EA | พื้นหลังหลักของหน้า (สีกระดาษธรรมชาติ/ไม่ฟอกขาว) |
| surface | #FBF8F3 | พื้นหลัง card/panel/table (อุ่นกว่า background เล็กน้อย) |
| border | #DCD3C4 | เส้นขอบ card, ตาราง, divider |
| text-primary | #33291F | ข้อความหลัก, หัวข้อ (น้ำตาลเข้มเกือบดำ ไม่ใช้ดำสนิท #000) |
| text-secondary | #6E6355 | ข้อความรอง, label, caption |
| text-disabled | #B3A996 | ข้อความ/องค์ประกอบที่ยังใช้งานไม่ได้ (placeholder เมนู) |

**กติกาการใช้สีสถานะ**: `success` / `warning` / `danger` ต้องใช้เฉพาะเพื่อสื่อความหมายเชิงสถานะเท่านั้น ห้ามนำไปใช้ตกแต่งทั่วไป (เช่น ห้ามใช้สี `danger` กับปุ่มที่ไม่ใช่ action อันตราย) เพื่อรักษาความน่าเชื่อถือของสัญญาณเตือนในระบบ

**Contrast**: ข้อความบนพื้นหลังต้องผ่านอย่างน้อย WCAG AA (4.5:1 สำหรับ body text, 3:1 สำหรับ heading ขนาดใหญ่) — เนื่องจากพาเลตต์เป็นโทนดินอิ่มตัวต่ำ ให้ตรวจ contrast จริงทุกครั้งที่นำสีไปจับคู่กับพื้นหลังใหม่ โดยเฉพาะ `text-secondary` และ `warning` บนพื้น `surface`

### 2.2 Typography

- **Heading/Body font (ไทย)**: "Sarabun", "IBM Plex Sans Thai", sans-serif — เส้นสายเรียบ อ่านง่าย ไม่มี serif หรือลวดลายประดับ
- **Heading/Body font (อังกฤษ/ตัวเลข)**: "Inter", system-ui, sans-serif — จับคู่กับ Sarabun ได้ลงตัว น้ำหนักเส้นใกล้เคียงกัน
- **Scale**: H1 30px (600) / H2 22px (600) / H3 18px (600) / Body 16px (400) / Small 14px (400) / Caption 12px (500)
- **หลักการ**: ใช้ **น้ำหนักตัวอักษร (weight)** และ **ขนาด** ในการสร้างลำดับชั้นของข้อมูล ไม่ใช้สีสันจัดจ้านหรือตัวเอียง/ขีดเส้นใต้เพื่อสร้าง hierarchy (สอดคล้องกับความเรียบแบบ Muji)
- Line-height: 1.5 สำหรับ body, 1.3 สำหรับ heading

### 2.3 Spacing & Layout

- **Spacing unit ฐาน**: 8px แต่เว้นระยะ (whitespace) โดยรวมให้ "กว้างกว่าปกติ" กว่าดีไซน์ทั่วไปหนึ่งระดับ (เช่น padding ภายใน card ใช้ 24px แทนที่จะเป็น 16px) เพื่อให้ได้ความรู้สึกโปร่ง สงบ ไม่แน่นจนอึดอัด
- **Border radius**: 4px สำหรับปุ่ม/input, 6px สำหรับ card/panel — โค้งเบาๆ พอให้ดูนุ่มนวล ไม่ใช่ทรงเหลี่ยมคมหรือมนกลมแบบ pill (หลีกเลี่ยง radius ที่มากเกินไปเพราะจะขัดกับความเรียบนิ่งของ Muji)
- **Elevation/Shadow**: ใช้ shadow ให้น้อยที่สุดเท่าที่จำเป็น เน้นแยกองค์ประกอบด้วย **เส้นขอบบาง (1px, สี border)** และ **ช่องว่าง** แทนเงา — ถ้าจำเป็นต้องมี shadow (เช่น modal/dropdown ลอยเหนือ content) ให้ใช้ shadow บางที่สุด `0 2px 6px rgba(51,41,31,0.06)`
- **Max content width**: 1280px, padding ข้าง 32px บนจอกว้าง / 16px บนจอเล็ก

---

## 3. UI Components & Patterns

- **ปุ่ม (Button)**
  - Primary: พื้นหลัง `primary` (#7A6A53) ตัวอักษร `surface` (ครีมอ่อน ไม่ใช่ขาวจัด), radius 4px, ไม่มี shadow, hover เข้มขึ้น ~10%
  - Secondary: พื้นโปร่งใส ขอบ 1px สี `primary`, ตัวอักษรสี `primary`
  - Disabled: พื้น `text-disabled`, ตัวอักษร `surface`, ไม่มี hover state
- **การ์ด (Card)**: พื้นหลัง `surface`, ขอบ 1px สี `border`, radius 6px, ไม่มี shadow เป็นค่าเริ่มต้น (ใช้ขอบแยกองค์ประกอบแทน), padding ภายใน 24px
- **Navigation**: แนะนำ **left rail แบบบาง** (ไม่ใช่ top bar สีทึบ) พื้นหลัง `surface` ตัวอักษร `text-primary`, active item ใช้พื้นหลัง `background` เข้มขึ้นเล็กน้อย + ตัวอักษรสี `primary` (ไม่ใช้แถบสีสดหรือ underline หนา) — ให้ความรู้สึกเป็นชั้นวางที่เรียบ ไม่ใช่แถบเมนูสีจัด
- **สถานะ/Badge**: แทนที่ pill สีสดด้วย **จุดกลม (status dot) ขนาดเล็ก + label ข้อความ** สีตามสถานะ วางคู่กัน แทนพื้นหลังสีเต็มแคปซูล เพื่อความเรียบ; ถ้าจำเป็นต้องใช้พื้นหลัง ให้ใช้สีอิ่มตัวต่ำมาก (tint 10-15%) ของสีสถานะเท่านั้น
- **Input/Form**: label วางด้านบน input เสมอ, กรอบ 1px สี `border`, radius 4px, focus state ใช้ขอบสี `primary` หนา 2px (ไม่ใช้ glow/shadow สีสด)
- **ตาราง (Table)**: ไม่ใช้ zebra stripe สีจัด ใช้เส้นแบ่งแถวบางสี `border` เท่านั้น, header ตัวหนา สี `text-secondary`
- **กราฟ/แผนภูมิ**: ใช้โทนดิน (`secondary`, `accent`, เฉด `primary`) เป็นสีหลักของกราฟ และ **สงวนสีสถานะ (`success`/`warning`/`danger`) ไว้เฉพาะจุดที่ต้องสื่อความหมายเชิงสถานะจริงๆ** เท่านั้น (เช่น เส้น threshold หรือจุดที่เกินค่าเตือน)
- **ไอคอน**: เส้นบาง (thin line, ~1.5px stroke) สีเดียว (monochrome, ใช้สี `text-primary` หรือ `text-secondary`) ไม่ใช้ไอคอนสีหลากสีหรือ filled icon เว้นแต่จุดที่ต้องเน้นสถานะ

---

## 4. UX Guidelines & Rules

1. **หนึ่งหน้าจอ เน้นหนึ่งเป้าหมายหลัก** — หลีกเลี่ยงการยัดข้อมูล/action ที่ไม่เกี่ยวข้องกันไว้ในมุมมองเดียว จัดกลุ่มด้วยช่องว่างแทนเส้นขอบเมื่อทำได้
2. **สีสถานะต้องคงความหมายเดียวเสมอ** — `danger` = วิกฤตจริงเท่านั้น, `warning` = ต้องเฝ้าระวังจริงเท่านั้น ห้ามใช้ซ้ำเพื่อความสวยงาม เพราะระบบนี้ผู้ใช้ต้องตัดสินใจเชิงสาธารณสุขจากสีที่เห็น ความสับสนมีต้นทุนสูงกว่าระบบทั่วไป
3. **ลำดับชั้นข้อมูลด้วยขนาด/น้ำหนัก ไม่ใช่สีสัน** — เมื่อพาเลตต์เป็นโทนดินอิ่มตัวต่ำ การใช้สีสร้าง hierarchy จะดูรกและขัดธีม ให้พึ่งพา typography scale และ spacing แทน
4. **Whitespace คือส่วนหนึ่งของดีไซน์ ไม่ใช่พื้นที่ว่างที่ต้องเติม** — อย่าลดระยะห่างเพื่อยัดข้อมูลเพิ่มเมื่อพื้นที่ไม่พอ ให้พิจารณาตัดข้อมูลส่วนรองออกไปหน้าอื่น/แท็บอื่นแทน
5. **Motion เบาและสงบ** — transition/animation ทั้งหมดใช้ duration 150-250ms ease-in-out เท่านั้น ห้ามมี animation ที่ดึงความสนใจ (bounce, flash, scale เกินจริง) ยกเว้น alert ระดับวิกฤตที่อนุญาตให้มี indicator กระพริบเบาๆ ได้เป็นกรณีพิเศษเพื่อความปลอดภัย
6. **Accessibility ต้องมาก่อนความเรียบ** — ถ้าโทนดินคู่ใดไม่ผ่าน contrast ratio ที่กำหนด (ดู 2.1) ให้ปรับความเข้ม/อ่อนของสีนั้นจนผ่านมาตรฐานก่อน ไม่ยอมลดทอน accessibility เพื่อความสวยงามของพาเลตต์
7. **ไม่ใช้ภาพถ่าย/illustration ตกแต่ง** — ยกเว้นกรณีจำเป็นเชิงข้อมูล (เช่น ไอคอนประกอบชนิดโรค) ให้เน้นข้อมูล/กราฟ/ตัวเลขเป็นองค์ประกอบภาพหลักแทนภาพประกอบ
8. **ข้อความแจ้งเตือนเขียนแบบสงบหนักแน่น** — ตามหลัก Voice & Tone ในข้อ 1 แม้เป็นการแจ้งเตือนระดับวิกฤต ให้สื่อสารข้อเท็จจริงและ action ที่ต้องทำ ไม่ใช้คำ/เครื่องหมายกระตุ้นความตื่นตระหนก

---

## Notes

- ร่างขึ้นจากทิศทางที่ผู้ใช้ระบุ: **Earth tone + Minimalist + Muji** ยังไม่มี hex code/โลโก้จริงจากผู้ใช้ — ค่าสีทั้งหมดเป็นการตีความจากทิศทางนี้ ถ้ามีแบรนด์จริง (โลโก้/สีจากแนวทางองค์กร) ภายหลัง ให้แก้ค่าในไฟล์นี้ แล้ว prototype รอบถัดไปจะอ้างอิงตามใหม่ทันที
- เกิดความตึงเครียดโดยธรรมชาติระหว่าง "Muji แบบเรียบสุดๆ" กับ "ระบบเฝ้าระวังโรคที่ต้องส่งสัญญาณอันตรายชัดเจน" — ไฟล์นี้แก้ด้วยการให้สีสถานะ (`success`/`warning`/`danger`) อิ่มตัวสูงกว่าพาเลตต์หลักเจตนา และสงวนไว้ใช้เฉพาะสถานะจริงเท่านั้น (ดูข้อ 4.2) ถ้าทดลองแล้วรู้สึกว่ายังไม่ชัดพอหรือชัดเกินจนขัดธีม แจ้งได้ จะปรับค่าความอิ่มตัวให้
